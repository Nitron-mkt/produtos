// Copie para config.js e preencha. config.js esta no .gitignore - NAO comite a chave.
window.PDP_CONFIG = {
  SUPABASE_URL: 'https://bwbeieumxcuomtrvlqxs.supabase.co',
  // Use uma chave publishable (sb_publishable_...) ou a anon key.
  // ATENCAO: hoje 89 tabelas deste projeto estao com RLS DESLIGADO. Enquanto isso
  // nao for corrigido, esta chave da acesso de leitura/escrita a dados de cliente.
  // Nao publique este front-end em URL publica antes de resolver o RLS.
  SUPABASE_KEY: 'COLE_A_CHAVE_AQUI',
};
